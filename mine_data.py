from tile import TileFlag

data_set = {
    {1: (
        # 타일 이미지
        {'image': 'Assets/Sprites/Tile/Block01Tileset.png'},

        # 타일 배치 및 플래그.
        {'small': ({'location': False},
                   {'flag': False}
        )}
    )}
}
